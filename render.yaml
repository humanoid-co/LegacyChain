services:
  - type: web
    name: legacychain-demo
    runtime: node
    plan: starter
    buildCommand: npm ci && npm --prefix frontend ci && npm run compile && npm --prefix frontend run build
    startCommand: node server.js
    healthCheckPath: /health
    autoDeploy: true
